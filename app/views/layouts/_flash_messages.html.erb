<div id="flash-messages" class="fixed top-4 right-4 z-50 space-y-2">
  <% flash.each do |type, message| %>
    <div class="<%= flash_background_color(type) %> max-w-sm transition-all duration-300 ease-in-out"
         data-controller="flash">
      <%= message %>
    </div>
  <% end %>
</div>
