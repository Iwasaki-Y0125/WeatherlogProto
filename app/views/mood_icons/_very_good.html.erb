<svg viewBox="0 0 24 24" fill="currentColor" class="w-full h-full">
  <!-- 背景の円 -->
  <circle cx="12" cy="12" r="10" fill="currentColor" opacity="0.15"/>
  <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" fill="none"/>

  <!-- (^^)の目 -->
  <g stroke="currentColor" stroke-width="2" stroke-linecap="round" fill="none">
    <!-- 左目 ^ -->
    <path d="M6 9l2-1.5 2 1.5"/>
    <!-- 右目 ^ -->
    <path d="M14 9l2-1.5 2 1.5"/>
  </g>

  <!-- スイカ口 -->
  <ellipse cx="12" cy="16" rx="5" ry="3" fill="currentColor" opacity="0.8"/>
  <ellipse cx="12" cy="16" rx="4" ry="2" fill="currentColor" opacity="0.3"/>

  <!-- 頬の赤み -->
  <circle cx="5" cy="12" r="1.5" fill="currentColor" opacity="0.3"/>
  <circle cx="19" cy="12" r="1.5" fill="currentColor" opacity="0.3"/>
</svg>
